# ── Core ────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings>=2.0.0
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
httpx>=0.27.0
aiosqlite==0.20.0
numpy<2.0
loguru>=0.7.0
PyMuPDF>=1.24.0
Pillow>=10.0.0
opencv-python>=4.9.0

# ── AI Providers (install what you use) ─────────────────────────
google-generativeai==0.8.3           # Gemini
# anthropic>=0.30.0                  # Claude (pip install anthropic)
# openai>=1.30.0                     # OpenAI SDK (optional, we use httpx)

# ── OCR Engines (install what you use) ──────────────────────────
# PaddleOCR — GPU (default printed-text engine)
paddlepaddle-gpu==3.3.1
paddleocr==2.8.1
# Tesseract — CPU fallback (pip install pytesseract, needs tesseract binary)
# pytesseract>=0.3.13
# EasyOCR — CPU/GPU alternative
# easyocr>=1.7.1
# Surya OCR — transformer-based
# surya-ocr>=0.6.0
# docTR — document text recognition
# python-doctr[torch]>=0.9.0

# ── Preprocessing ──────────────────────────────────────────────
deskew>=1.5.0

# ── Table OCR (PaddleOCR PP-Structure) ─────────────────────────
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ── Qwen2.5-VL handwritten OCR ────────────────────────────────
torch==2.7.1+cu128
torchvision==0.22.1+cu128
transformers>=4.51
qwen-vl-utils
accelerate

# ── Evaluation + Testing ──────────────────────────────────────
jiwer>=3.0.0
pytest-cov>=4.1.0
